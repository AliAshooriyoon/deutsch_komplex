-- CreateTable
CREATE TABLE "example" (
    "id" TEXT NOT NULL,
    "level" TEXT NOT NULL,
    "topic" TEXT NOT NULL,
    "content" TEXT NOT NULL,

    CONSTRAINT "example_pkey" PRIMARY KEY ("id")
);
